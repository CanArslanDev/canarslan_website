import 'package:flutter/material.dart';

abstract class AppBorderRadius {
  static const BorderRadius extraSmall = BorderRadius.all(Radius.circular(5));
  static const BorderRadius small = BorderRadius.all(Radius.circular(8));
  static const BorderRadius mediumSmall = BorderRadius.all(Radius.circular(12));
  static const BorderRadius medium = BorderRadius.all(Radius.circular(20));
  static const BorderRadius mediumLarge = BorderRadius.all(Radius.circular(24));
  static const BorderRadius large = BorderRadius.all(Radius.circular(26));
  static const BorderRadius extraLarge = BorderRadius.all(Radius.circular(30));
}
